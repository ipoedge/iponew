services:
  - type: web
    name: fetch-external-content
    env: node
    buildCommand: npm install
    startCommand: node server.js  # Update this line if necessary
    workingDir: /opt/render/project
